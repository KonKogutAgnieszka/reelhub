## Tests

- **API layer** — `mapMovie` verifies snake_case → camelCase mapping from TMDB responses
- **UI layer** — `MovieCard` verifies rendering, correct href, and poster fallback
- **Hook layer** — `useMovieFilters` verifies URL params are read correctly
