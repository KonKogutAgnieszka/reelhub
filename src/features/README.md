## Features

Feature-base modules

## Structure

- `api/` API funtions and response types
- `components/` UI components per feature
- `hooks/` custom hooks
- `types` domain types

## Current features

- `movies/` — movie browsing, filtering, and details
